// Virtual-module stub (overridden by the offline-sw plugin at build time).
// Kept on disk so `vite dev` and typecheck resolve the import.
export const OFFLINE_SHELL_ASSETS: string[] = ["/index.html"];
